
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile within the NEU Library Visitor App, storing their institutional details and access status.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity. This typically corresponds to the Firebase Authentication UID."
        },
        "email": {
          "type": "string",
          "description": "The user's institutional email address (@neu.edu.ph), used for login and identification.",
          "format": "email"
        },
        "fullName": {
          "type": "string",
          "description": "The user's full name, typically retrieved from their Google profile."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the application (e.g., admin, regular user).",
          "items": {
            "type": "string"
          }
        },
        "collegeOffice": {
          "type": "string",
          "description": "The college or office the user belongs to, selected during the initial onboarding process."
        },
        "isSetupComplete": {
          "type": "boolean",
          "description": "Indicates whether the user has completed their initial onboarding setup (e.g., selected college/office). Set to 'false' on first login."
        },
        "isBlocked": {
          "type": "boolean",
          "description": "Indicates whether the user is blocked by an administrator from entering the library. Defaults to 'false'."
        }
      },
      "required": [
        "id",
        "email",
        "fullName",
        "role",
        "collegeOffice",
        "isSetupComplete",
        "isBlocked"
      ]
    },
    "Visit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Visit",
      "type": "object",
      "description": "Records a single visit of a user to the NEU Library, including the reason and timestamp.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Visit entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile who made this visit."
        },
        "fullName": {
          "type": "string",
          "description": "Denormalized full name of the visitor."
        },
        "email": {
          "type": "string",
          "description": "Denormalized email of the visitor."
        },
        "collegeOffice": {
          "type": "string",
          "description": "Denormalized college or office of the visitor."
        },
        "reason": {
          "type": "string",
          "description": "The primary reason for the user's visit to the library."
        },
        "timestamp": {
          "type": "string",
          "description": "The date and time when the user's visit was recorded.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "fullName",
        "email",
        "collegeOffice",
        "reason",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles. Access is restricted to self or admin."
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "Visit",
          "schema": {
            "$ref": "#/backend/entities/Visit"
          },
          "description": "Global collection of all library visits. Users can create visits for themselves, admins can view all."
        }
      }
    ],
    "reasoning": "Moving visits to a top-level collection simplifies global analytics and reporting for administrators while still maintaining strict user-based ownership via the 'userId' field for security rules."
  }
}
